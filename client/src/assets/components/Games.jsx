import GameList from "./videogames.jsx";

const Games = () => {
    return (
        <div id ="card-Example"> 
            {GameList()}
            {GameList()}
            {GameList()}
            {GameList()}
            {GameList()}
            {GameList()}
        </div>
  )  ;
};

export default Games;
